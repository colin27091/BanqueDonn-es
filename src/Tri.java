import java.util.ArrayList;
import java.util.Hashtable;

public class Tri {
	
	
	/*Tri Date Croissante (plus recent au plus vieux)*/
	static Hashtable<Integer, Pic> dateC(Hashtable<Integer, Pic> oldfilter) {
		Hashtable<Integer, Pic> newfilter = new Hashtable<Integer, Pic>();
		return newfilter;
	}

	/*Tri Date Decroissante (plus vieux au plus recent)*/
	static Hashtable<Integer, Pic> dateD(Hashtable<Integer, Pic> oldfilter) {
		Hashtable<Integer, Pic> newfilter = new Hashtable<Integer, Pic>();
		return newfilter;
	}
	
	/*Tri Nom Croissant (ordre alphabetique) */
	static Hashtable<Integer, Pic> nomC(Hashtable<Integer, Pic> oldfilter) {
		Hashtable<Integer, Pic> newfilter = new Hashtable<Integer, Pic>();
		return newfilter;
	}
	
	/*Tri Nom Decroissant (inverse alphabetique)*/
	static Hashtable<Integer, Pic> nomD(Hashtable<Integer, Pic> oldfilter) {
		Hashtable<Integer, Pic> newfilter = new Hashtable<Integer, Pic>();
		return newfilter;
	}
	
	/*Tri Taille Croissante (plus petite a plus grande)*/
	static Hashtable<Integer, Pic> tailleC(Hashtable<Integer, Pic> oldfilter) {
		Hashtable<Integer, Pic> newfilter = new Hashtable<Integer, Pic>();
		return newfilter;
	}
	
	/*Tri Taille Decroissante (plus grande a plus petite)*/
	static Hashtable<Integer, Pic> tailleD(Hashtable<Integer, Pic> oldfilter) {
		Hashtable<Integer, Pic> newfilter = new Hashtable<Integer, Pic>();
		return newfilter;
	}
}
