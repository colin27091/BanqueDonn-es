import java.util.ArrayList;
import java.util.Hashtable;

public class Filtrage {
	
	/*Filtrage pour les differents tag passe en parametre sour forme de string*/
	static Hashtable<Integer, Pic> setfilter(Hashtable<Integer, Pic> oldfilter, String... tags) {
		Hashtable<Integer, Pic> newfilter = new Hashtable<Integer, Pic>();
		return newfilter;
	}
	
	/*Filtrage pour un nom recherche*/
	static Hashtable<Integer, Pic> searchField(Hashtable<Integer, Pic> oldfilter) {
		Hashtable<Integer, Pic> newfilter = new Hashtable<Integer, Pic>();
		return newfilter;
	}

}
